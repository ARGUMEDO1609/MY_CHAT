 <%= turbo_frame_tag 'chats_controller' do %> 
<main class='flex w-full h-screen bg-[#86198f]'> 
  <%= render 'shared/side_bar'%>

<section class= "flex flex-col items-center justify-center gap-5 h-screen w-full"> 
    <%= image_tag("04.png", width: 100) %>
    <h1 class="font-bold text-4xl text-white my-4">VIOLETH CHAT</h1>
    <p class="text-center font-semibold text-black">CHAT LIVE</p>
  </section>
</main>
<% end %>