<%= turbo_frame_tag "message_#{@message.id}", class:"bg_gray-300 px-3 py-2 flex justify-between rounded items-center gap-2 shadow" do %>
 <%= render 'messages/form', { chat: @chat, message: @message } %>
 <% end %>