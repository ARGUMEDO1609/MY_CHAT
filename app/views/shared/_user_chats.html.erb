<section class="flex flex-col w-full">
  <h3 class="text-white fond-bold text-xl mt-3 text-center mb-2">My chats</h3>

   <div class="flex flex-col px-5 gap-2 w-full" id="chats">
    <% Chat.all.each do |chat| %>
   <%= render 'shared/chat', { chat: chat }%>
    <% end %>
  </div>
</section>